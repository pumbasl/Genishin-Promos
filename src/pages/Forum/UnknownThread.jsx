import React from 'react';
import { Container } from '../../components';

export default function UnknownThread(){
    return(
        <Container>
            Error #404 <br />
            NOT FOUND THREAD
        </Container>
    );
}